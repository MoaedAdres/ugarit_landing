---
alwaysApply: false
path: "views/dashboard/**"
---

You are an expert in TypeScript, Node.js, Next.js App Router, React, Shadcn UI, Radix UI and Tailwind for the dashboard views section.

## Dashboard Views Code Style and Structure
- Write concise, technical TypeScript code with accurate examples.
- Use functional and declarative programming patterns; avoid classes.
- Prefer iteration and modularization over code duplication.
- Use descriptive variable names with auxiliary verbs (e.g., isLoading, hasError, isEditing).
- Structure files: exported component, subcomponents, helpers, static content, types.

## Dashboard Views Naming Conventions
- Use lowercase with dashes for directories (e.g., views/dashboard/content-management).
- Favor named exports for components.
- Use descriptive names for dashboard view components (e.g., DashboardSidebar, ContentManagementView).

## TypeScript Usage for Dashboard Views
- Use TypeScript for all code; prefer interfaces over types.
- Avoid enums; use maps instead.
- Use functional components with TypeScript interfaces.
- Define clear interfaces for dashboard view data structures.

## Dashboard Views Syntax and Formatting
- Use the "function" keyword for pure functions.
- Avoid unnecessary curly braces in conditionals; use concise syntax for simple statements.
- Use declarative JSX.
- Keep dashboard view components focused and single-purpose.

## Dashboard Views UI and Styling
- Use Shadcn UI, Radix, and Tailwind for components and styling.
- Implement responsive design with Tailwind CSS; use a mobile-first approach.
- Ensure dashboard view components work well on different screen sizes.
- Use consistent spacing and typography throughout the dashboard views.

## Dashboard Views Performance Optimization
- Minimize 'use client', 'useEffect', and 'setState'; favor React Server Components (RSC).
- Wrap client components in Suspense with fallback.
- Use dynamic loading for non-critical dashboard view components.
- Optimize images: use WebP format, include size data, implement lazy loading.
- Implement proper loading states for dashboard view data.

## Dashboard Views-Specific Conventions
- Use 'nuqs' for URL search parameter state management in dashboard view filters.
- Optimize Web Vitals (LCP, CLS, FID) for dashboard view pages.
- Limit 'use client' in dashboard views:
  - Favor server components and Next.js SSR for data fetching.
  - Use only for Web API access in small interactive components.
  - Avoid for data fetching or state management.
- Implement proper error boundaries for dashboard view sections.
- Use consistent form validation and error handling.

## Dashboard Views Data Management
- Implement proper loading states for all dashboard view operations.
- Use optimistic updates where appropriate.
- Handle errors gracefully with user-friendly messages.
- Implement proper caching strategies for dashboard view data.

Follow Next.js docs for Data Fetching, Rendering, and Routing in dashboard views context.